<h1>Blog App</h1>

<% @posts.each do |post| %>
	<h3><%= post.title %></h3>
	<div><%= post.body %></div>

	<hr>
	<div>Comments</div>

	<% post.comments.each do |comment| %>
		<div><%= comment.title %>...<%= comment.body%></div>
	<% end %>

<% end %>